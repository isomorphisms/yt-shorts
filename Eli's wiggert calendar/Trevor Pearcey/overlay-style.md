# Caustic overlay style

The caustic curve must read as an annotation laid over the computed field.

Use a high-contrast dashed stroke with a small dark backing/shadow and the explicit label `caustic overlay`. Do not use a solid line that could be mistaken for a feature produced by `wegert_color_complex`.
