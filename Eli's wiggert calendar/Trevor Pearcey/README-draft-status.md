# Draft status

The first MP4 is retained as a draft reference, but the next render should implement the timing/overlay revision in `revision-notes.md` before being treated as the preferred preview.

Preferred visual sequence:

1. compute/build the Pearcey field while the camera is still relatively close;
2. zoom out faster than the first draft;
3. hold the complete zoomed-out field unmarked for several seconds;
4. only then introduce the dashed caustic overlay as an explanatory annotation;
5. hold again with the annotation visible.
